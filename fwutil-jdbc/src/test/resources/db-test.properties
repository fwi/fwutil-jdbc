# using a prefix, several databases can be configured using this one properties file.

# the sqllog parameter ensures HSQL logs all database quries
db.test.dataSource.url=jdbc:hsqldb:mem:testdb;hsqldb.sqllog=3;shutdown=true
db.test.dataSource.user=sa
db.test.dataSource.password=
db.test.poolName=testdb
db.test.initializationFailFast=true

# same kind of database as testdb but without the HSQL logs

db.test2.dataSource.url=jdbc:hsqldb:mem:testdb2;shutdown=true
db.test2.dataSource.user=sa
db.test2.dataSource.password=
#db.test2.registerMbeans=false
#db.test2.poolName=testdb2
#db.test2.initializationFailFast=true
